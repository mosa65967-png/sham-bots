{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npx prisma generate && npm run build",
    "watchPatterns": ["src/**/*", "prisma/**/*", "package.json", "railway.json", ".nvmrc"]
  },
  "deploy": {
    "startCommand": "npx prisma generate && node prisma/push.js && next start",
    "healthcheckPath": "/api/v1/governorates",
    "healthcheckTimeout": 60
  }
}
